apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: k8s-microsvc
spec:
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
