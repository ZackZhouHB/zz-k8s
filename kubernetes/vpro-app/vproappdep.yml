apiVersion: apps/v1
kind: Deployment
metadata:
  name: vproapp
  labels: 
    app: vproapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vproapp
  template:
    metadata:
      labels:
        app: vproapp
    spec:
      containers:
      - name: vproapp
        image: zackz001/redo20-e4-app:latest   # github image
        ports:
        - name: vproapp-port
          containerPort: 8080

#      initContainers:                      # init container as dependency to query and wait
#      - name: init-mydb                    # app will depend on db and memcache ready
#        image: busybox
#        command: ['sh', '-c', 'until nslookup vprodb; do echo waiting for mydb; sleep 2; done;']
#      - name: init-memcache
#        image: busybox
#        command: ['sh', '-c', 'until nslookup vprocache01; do echo waiting for mydb; sleep 2; done;']


